{
  "name": "authentication-ms",
  "version": "1.0.0",
  "description": "micro-serviço de autenticação com nodejs, express e postgresql",
  "main": "./dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./",
    "build": "tsc -p ."
  },
  "author": "Henrique de Almeida",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^16.9.1",
    "typescript": "^4.4.3"
  }
}
